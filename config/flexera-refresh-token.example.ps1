# Flexera One user API authentication via API Refresh Token.
# Copy this file outside version control or load these values from a secret store.

$env:FLEXERA_REFRESH_TOKEN = 'replace-with-your-api-refresh-token'
$env:FLEXERA_ZONE = 'EU' # EU, NAM or APAC

# Optional overrides:
# $env:FLEXERA_TOKEN_URL = 'https://login.flexera.eu/oidc/token'
# $env:FLEXERA_ORG_ID = '12345'
